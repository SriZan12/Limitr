package com.example.limitr.ui.navigation

import androidx.compose.runtime.Composable

/**
 * Placeholder for Compose navigation setup.
 *
 * XML/NavComponent flow remains the source of truth during incremental migration.
 */
@Composable
fun LimitrNavHost() {
    // Intentionally empty until XML screens are migrated one-by-one.
}
